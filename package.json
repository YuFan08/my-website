{
  "scripts": {
    "vercel-build": "npm run docs:build",
    "build": "vitepress build",
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "pub": "git add . && git commit -m 'update' && git push origin main"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.31"
  }
}
